# Data

Place the assessment-provided CSV files in this folder:

- `train_test.csv`
- `validation.csv`
- `validation_predictions_template.csv`
- `december_chart_inputs.csv`

The raw assessment CSV files are excluded from GitHub by `.gitignore`.
